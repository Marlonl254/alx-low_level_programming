#include <stdio.h>

/**
 * main - Starting point of our program
 *
 * Return: zero if main is successful
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
