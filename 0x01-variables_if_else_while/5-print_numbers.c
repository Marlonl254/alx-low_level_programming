#include <stdio.h>

/**
 * main - entry point of our program
 *
 * Return: zero = successful execution
 */

int main(void)
{
	char i;

	for (i = '0'; i <= '9'; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
